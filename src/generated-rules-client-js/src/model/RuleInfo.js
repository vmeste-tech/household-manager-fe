/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VoteDto from './VoteDto';

/**
 * The RuleInfo model module.
 * @module model/RuleInfo
 * @version v0
 */
class RuleInfo {
    /**
     * Constructs a new <code>RuleInfo</code>.
     * @alias module:model/RuleInfo
     */
    constructor() { 
        
        RuleInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>RuleInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RuleInfo} obj Optional instance to populate.
     * @return {module:model/RuleInfo} The populated <code>RuleInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RuleInfo();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('penaltyAmount')) {
                obj['penaltyAmount'] = ApiClient.convertToType(data['penaltyAmount'], 'Number');
            }
            if (data.hasOwnProperty('cronExpression')) {
                obj['cronExpression'] = ApiClient.convertToType(data['cronExpression'], 'String');
            }
            if (data.hasOwnProperty('voteResult')) {
                obj['voteResult'] = ApiClient.convertToType(data['voteResult'], [VoteDto]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RuleInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RuleInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['cronExpression'] && !(typeof data['cronExpression'] === 'string' || data['cronExpression'] instanceof String)) {
            throw new Error("Expected the field `cronExpression` to be a primitive type in the JSON string but got " + data['cronExpression']);
        }
        if (data['voteResult']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['voteResult'])) {
                throw new Error("Expected the field `voteResult` to be an array in the JSON data but got " + data['voteResult']);
            }
            // validate the optional field `voteResult` (array)
            for (const item of data['voteResult']) {
                VoteDto.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {String} id
 */
RuleInfo.prototype['id'] = undefined;

/**
 * @member {String} name
 */
RuleInfo.prototype['name'] = undefined;

/**
 * @member {String} description
 */
RuleInfo.prototype['description'] = undefined;

/**
 * @member {module:model/RuleInfo.StatusEnum} status
 */
RuleInfo.prototype['status'] = undefined;

/**
 * @member {Number} penaltyAmount
 */
RuleInfo.prototype['penaltyAmount'] = undefined;

/**
 * @member {String} cronExpression
 */
RuleInfo.prototype['cronExpression'] = undefined;

/**
 * @member {Array.<module:model/VoteDto>} voteResult
 */
RuleInfo.prototype['voteResult'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
RuleInfo['StatusEnum'] = {

    /**
     * value: "ACCEPTED"
     * @const
     */
    "ACCEPTED": "ACCEPTED",

    /**
     * value: "REJECTED"
     * @const
     */
    "REJECTED": "REJECTED",

    /**
     * value: "VOTING"
     * @const
     */
    "VOTING": "VOTING"
};



export default RuleInfo;

